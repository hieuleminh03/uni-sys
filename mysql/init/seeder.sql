-- Simple seed script for admin account (placeholder)
-- 
-- This file exists as a placeholder and will be run during MySQL initialization,
-- but we will let Spring Boot handle the schema creation and data seeding
-- through JPA since it has the full entity definitions and can manage migrations.
--
-- This prevents errors that would stop the MySQL container when tables don't exist yet.

SELECT 'Database initialized. Tables will be created by the application.' AS message;
